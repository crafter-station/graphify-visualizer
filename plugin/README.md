# plugin/

Obsidian plugin source root.

**Status:** empty — Fase 0 not started.

When scaffolding starts, expect roughly:

```text
plugin/
├── manifest.json
├── package.json
├── tsconfig.json
├── esbuild.config.mjs   # or equivalent
├── styles.css
├── src/
│   └── main.ts          # Plugin + ItemView registration
└── README.md            # local install / symlink into .obsidian/plugins
```

Parent docs: [`../README.md`](../README.md) · [`../docs/ROADMAP.md`](../docs/ROADMAP.md)
